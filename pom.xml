<project xmlns="http://maven.apache.org/POM/4.0.0"
		 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		 xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>lv.id.bonne</groupId>
	<artifactId>dragonfights</artifactId>
	<version>${revision}</version>

	<name>DragonFights</name>
	<description>An addon for BentoBox plugin which allows to enable fights with ender dragons per island.</description>

	<url>https://github.com/BONNePlayground/DragonFights</url>
	<inceptionYear>2020</inceptionYear>

	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
		<java.version>21</java.version>
		<powermock.version>1.7.4</powermock.version>
		<!-- Paper 1.21.11 only; no Spigot -->
		<paper.version>1.21.11-R0.1-SNAPSHOT</paper.version>
		<!-- 3.10.1 is latest in Maven; 3.11.1 not yet published. Addon API compatible with 3.11.x -->
		<bentobox.version>3.10.1</bentobox.version>
		<level.version>2.5.0</level.version>
		<!-- Revision variable removes warning about dynamic version -->
		<revision>${build.version}-SNAPSHOT</revision>
		<!-- This allows to change between versions and snapshots. -->
		<build.version>1.9.0</build.version>
		<build.number>-LOCAL</build.number>
	</properties>

	<profiles>
		<!-- Local profile: use clean version in JAR name (DragonFights-1.9.0.jar) for easier verification -->
		<profile>
			<id>local</id>
			<activation>
				<activeByDefault>true</activeByDefault>
			</activation>
			<properties>
				<revision>${build.version}</revision>
				<build.number></build.number>
			</properties>
		</profile>
		<!-- Build profile is activated by BUILD_NUMBER variable. It replaces
			0 with correct build number. -->
		<!-- This profile will be used only if BUILD_NUMBER environment variable
			exists. -->
		<profile>
			<id>ci</id>
			<activation>
				<property>
					<name>env.BUILD_NUMBER</name>
				</property>
			</activation>
			<properties>
				<!-- Override only if necessary -->
				<build.number>-b${env.BUILD_NUMBER}</build.number>
				<!-- GIT_BRANCH -->
			</properties>
		</profile>
		<!-- master profile is activated by GIT_BRANCH variable. It removes
			'-SNAPSHOT' at the end of -->
		<!-- ${build.version} -->
		<!-- This profile will be used only if environment variable GIT_BRANCH
			is origin/master. -->
		<profile>
			<id>master</id>
			<activation>
				<property>
					<name>env.GIT_BRANCH</name>
					<value>origin/master</value>
				</property>
			</activation>
			<properties>
				<!-- Override only if necessary -->
				<revision>${build.version}</revision>
				<!-- Empties build.number value -->
				<build.number></build.number>
			</properties>
			<distributionManagement>
				<repository>
					<id>github</id>
					<url>https://maven.pkg.github.com/BONNePlayground/DragonFights</url>
				</repository>
			</distributionManagement>
		</profile>
	</profiles>

	<scm>
		<connection>scm:git:https://github.com/BONNePlayground/DragonFights.git</connection>
		<developerConnection>scm:git:git@github.com:BONNePlayground/DragonFights.git</developerConnection>
		<url>https://github.com/BONNePlayground/DragonFights</url>
	</scm>

	<ciManagement>
		<system>jenkins</system>
		<url>http://ci.codemc.io/job/BONNePlayground/job/DragonFights</url>
	</ciManagement>

	<issueManagement>
		<system>GitHub</system>
		<url>https://github.com/BONNePlayground/DragonFights/issues</url>
	</issueManagement>

	<repositories>
		<repository>
			<id>bentobox</id>
			<url>https://repo.codemc.io/repository/bentoboxworld/</url>
		</repository>
		<repository>
			<id>maven-snapshots</id>
			<url>https://repository.apache.org/content/repositories/snapshots/</url>
		</repository>
		<repository>
			<id>papermc</id>
			<url>https://repo.papermc.io/repository/maven-public/</url>
		</repository>
		<repository>
			<id>spigot-repo</id>
			<url>https://hub.spigotmc.org/nexus/content/repositories/snapshots</url>
		</repository>
		<repository>
			<id>codemc-repo</id>
			<url>https://repo.codemc.io/repository/maven-public</url>
		</repository>
		<repository>
			<id>bytecode</id>
			<url>https://repo.bytecode.space/repository/maven-public/</url>
		</repository>
		<repository>
			<id>stellardrift</id>
			<url>https://repo.stellardrift.ca/repository/maven-public/</url>
		</repository>
	</repositories>

	<pluginRepositories>
		<pluginRepository>
			<id>bytecode</id>
			<url>https://repo.bytecode.space/repository/maven-public/</url>
		</pluginRepository>
	</pluginRepositories>

	<distributionManagement>
		<snapshotRepository>
			<id>codemc-snapshots</id>
			<url>https://repo.codemc.io/repository/maven-snapshots</url>
		</snapshotRepository>
		<repository>
			<id>codemc-releases</id>
			<url>https://repo.codemc.io/repository/maven-releases</url>
		</repository>
	</distributionManagement>

	<dependencies>
		<dependency>
			<groupId>org.jetbrains</groupId>
			<artifactId>annotations</artifactId>
			<version>23.0.0</version>
			<scope>provided</scope>
		</dependency>
		<dependency>
			<groupId>io.papermc.paper</groupId>
			<artifactId>paper-api</artifactId>
			<version>${paper.version}</version>
			<scope>provided</scope>
		</dependency>
		<dependency>
			<groupId>org.spigotmc</groupId>
			<artifactId>plugin-annotations</artifactId>
			<version>1.2.3-SNAPSHOT</version>
			<scope>provided</scope>
		</dependency>
		<!-- Mojang-mapped NMS for Paper 1.21.11 (run paper-nms:init once to generate) -->
		<dependency>
			<groupId>ca.bkaw</groupId>
			<artifactId>paper-nms</artifactId>
			<version>1.21.11-SNAPSHOT</version>
			<scope>provided</scope>
		</dependency>
		<!-- Resolve Keyable/serialization used by Registry (paper-nms references but does not embed) -->
		<dependency>
			<groupId>com.mojang</groupId>
			<artifactId>datafixerupper</artifactId>
			<version>6.0.6</version>
			<scope>provided</scope>
		</dependency>
		<!-- BentoBox: latest supporting Paper 1.21.11 -->
		<dependency>
			<groupId>world.bentobox</groupId>
			<artifactId>bentobox</artifactId>
			<version>${bentobox.version}</version>
			<scope>provided</scope>
		</dependency>
		<dependency>
			<groupId>world.bentobox</groupId>
			<artifactId>level</artifactId>
			<version>${level.version}</version>
			<scope>provided</scope>
		</dependency>
	</dependencies>

	<build>
		<!-- By default ${revision} is ${build.version}-SNAPSHOT -->
		<!-- If GIT_BRANCH variable is set to origin/master, then it will
			be only ${build.version}. -->

		<!-- By default ${build.number} is -LOCAL. -->
		<!-- If the BUILD_NUMBER variable is set, then it will be -b[number]. -->
		<!-- If GIT_BRANCH variable is set to origin/master, then it will
			be the empty string. -->
		<finalName>${project.name}-${revision}${build.number}</finalName>

		<defaultGoal>clean package</defaultGoal>
		<resources>
			<resource>
				<directory>src/main/resources</directory>
				<filtering>true</filtering>
			</resource>
			<resource>
				<directory>src/main/resources/locales</directory>
				<targetPath>./locales</targetPath>
				<filtering>false</filtering>
			</resource>
		</resources>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-enforcer-plugin</artifactId>
				<version>3.4.1</version>
				<executions>
					<execution>
						<id>require-java-21</id>
						<goals>
							<goal>enforce</goal>
						</goals>
						<phase>validate</phase>
						<configuration>
							<rules>
								<requireJavaVersion>
									<message>Project requires JDK 21 to build (Paper API and paper-nms use Java 21). Set JAVA_HOME to JDK 21 (e.g. export JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64).</message>
									<version>[21,)</version>
								</requireJavaVersion>
							</rules>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>ca.bkaw</groupId>
				<artifactId>paper-nms-maven-plugin</artifactId>
				<version>1.4.10</version>
				<configuration>
					<gameVersion>1.21.11</gameVersion>
				</configuration>
				<executions>
					<execution>
						<id>init</id>
						<phase>initialize</phase>
						<goals>
							<goal>init</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-clean-plugin</artifactId>
				<version>3.1.0</version>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-resources-plugin</artifactId>
				<version>3.1.0</version>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.7.0</version>
				<configuration>
					<source>${java.version}</source>
					<target>${java.version}</target>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-jar-plugin</artifactId>
				<version>3.1.0</version>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-source-plugin</artifactId>
				<version>3.0.1</version>
				<executions>
					<execution>
						<id>attach-sources</id>
						<goals>
							<goal>jar-no-fork</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-install-plugin</artifactId>
				<version>2.5.2</version>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-deploy-plugin</artifactId>
				<version>2.8.2</version>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-shade-plugin</artifactId>
				<version>3.6.0</version>
				<configuration>
					<minimizeJar>true</minimizeJar>
					<artifactSet>
						<includes>
							<!-- No external NMS modules; 1.21 code is in-tree -->
						</includes>
					</artifactSet>
					<transformers>
						<!-- Add a transformer to exclude any other manifest files (possibly from dependencies). -->
						<transformer implementation="org.apache.maven.plugins.shade.resource.DontIncludeResourceTransformer">
							<resource>MANIFEST.MF</resource>
						</transformer>
						<!-- Add a transformer to include your custom manifest file. -->
						<transformer implementation="org.apache.maven.plugins.shade.resource.IncludeResourceTransformer">
							<resource>META-INF/MANIFEST.MF</resource>
							<file>src/main/resources/META-INF/MANIFEST.MF</file>
						</transformer>
					</transformers>
				</configuration>
				<executions>
					<execution>
						<id>shade</id>
						<goals>
							<goal>shade</goal>
						</goals>
						<phase>package</phase>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
</project>